# WIP — superseded

This vendored bridge port is ON HOLD: the chosen approach is to run bridge
mode on marin-community/harbor@main directly (a second venv), keeping this
fork for LOCAL apptainer mode only. Branch kept for reference; do not pin.
